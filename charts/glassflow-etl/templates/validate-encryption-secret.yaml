{{- if and .Values.global.encryption.enabled (not .Values.global.encryption.existingSecret.name) -}}
{{- fail "global.encryption.enabled=true requires global.encryption.existingSecret.name to be set. Chart-managed encryption secret generation was removed in 0.5.12. See migrations/MIGRATION-0.5.11-TO-0.5.12.md." -}}
{{- end -}}
