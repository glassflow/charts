apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glassflow-etl.fullname" . }}-test-connection"
  labels:
    {{- include "glassflow-etl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glassflow-etl.fullname" . }}:{{ .Values.api.service.port }}']
  restartPolicy: Never
