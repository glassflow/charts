{{- if and .Values.api.encryption.enabled (not .Values.api.encryption.existingSecret.name) .Values.api.encryption.createSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.api.encryption.secretName | default (printf "%s-encryption-key" .Release.Name) }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{ include "glassflow-etl.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
type: Opaque
stringData:
  encryption-key: {{ randAlphaNum 32 | quote }}
{{- end }}
